<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8F9FA">

    <!-- 1. Peta di bagian atas -->
    <fragment
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 2. Header Dinamis -->
    <RelativeLayout
        android:id="@+id/layoutHeaderStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/map">

        <TextView
            android:id="@+id/tvStatusTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Daftar Bank Sampah Terdekat"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"/>

        <ImageButton
            android:id="@+id/btnBackToList"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_alignParentEnd="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_visibility_off"
            android:visibility="gone"
            app:tint="@color/green" />
    </RelativeLayout>

    <!-- 3. RecyclerView DAFTAR TEMPAT (Bank Sampah) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewBankSampah"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/layoutHeaderStatus"
        app:layout_constraintBottom_toBottomOf="parent"
        android:visibility="visible"/>

    <!-- 4. RecyclerView INPUT SAMPAH (Muncul setelah klik) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewInputSampah"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/layoutHeaderStatus"
        app:layout_constraintBottom_toTopOf="@id/btn_setorkan_langsung"
        android:visibility="gone"
        android:clipToPadding="false"
        android:paddingBottom="8dp"/>

    <!-- 5. Tombol Setorkan -->
    <Button
        android:id="@+id/btn_setorkan_langsung"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:layout_margin="16dp"
        android:text="Setorkan"
        android:backgroundTint="#1B5E20"
        android:textStyle="bold"
        android:visibility="gone"
        app:cornerRadius="12dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>